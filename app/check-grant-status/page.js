import GrantStatusPage from "./check-status";

export const metadata = {
  title: "Check grant status",
  description: "Enter details to check your grant application approval status",
};

const GrantStatus = () => {
  return <GrantStatusPage />;
};

export default GrantStatus;
